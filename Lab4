/* 
1) Написать функции, вычисляющие периметр и площадь треугольника по значениям длин трёх его сторон.
2) Разместить их в отдельном модуле (отличном от того, где располагается функция main( )). 
3) Ввести с консоли значения длин сторон треугольника (с проверкой на корректность неравенства треугольника)
4) и выдать на консоль вычисленные с помощью написанных функций значения его периметра и площади. */

#include <stdio.h>
#include "mylib.h"

int main()
{
    int a, b, c, P;
    double S;

    //Вводим значения для сторон треугольника и выводим их на экран
    printf("Введите значение для 1 стороны: ");
    scanf("%d", &a);

    printf("Введите значение для 2 стороны: ");
    scanf("%d", &b);

    printf("Введите значение для 3 стороны: ");
    scanf("%d", &c); 

    // Проверка неравенств сторон треугольника, ввод и вывод периметра и площади
    if (a + b > c && a + c > b && b + c > a)
    {
        P = Perimeter(a, b, c);
        S = Area(a, b, c);
       
        printf("Периметр равен: %d\n", P);
        printf("Площадь равна: %.2lf\n", S);
    }
    else
    {
        printf("Таких сторон у треугольника быть не может!.\n");
    }
    
    return 0;
}
