/*
1)Объявить тип-структуру humen, включающую имя человека, фамилию и год рождения.
2)Объявить два массива из четырёх элементов типа humen.
3)Ввести с консоли или из файла элементы одного массива и построить на их основе другой, упорядочив элементы по годам рождения.
4)Вывести результат.
*/

#include <stdio.h>

/*Создаем структуру характеристики человека для дальнейшего использования*/
struct humen 
{
    char Name[50];
    char Lastname[50];
    int Year;
};


int main()
{
    int n;
    
    printf("Введите размерность массива: ");
    scanf("%d", &n);

    struct humen mass[n];
    
    /*Используем цикл, чтобы узнать Имя, Фамилию и Год людей*/

    for(int i = 0; i < n; i++)
    {
        printf("Введите имя: ");
        scanf("%s", mass[i].Name);
        printf("Введите фамилию: ");
        scanf("%s", mass[i].Lastname);
        printf("Введите год рождения: ");
        scanf("%d", &mass[i].Year);
    }

    /*Используем два цикла, чтобы упорядочить людей по их Году Рождения*/
    for(int i = 0; i < n; i++)
    {
        for(int j = i + 1; j < n; j++)
        {
            /*Используется метод пузырьком*/
            if(mass[i].Year > mass[j].Lastname)
            {
                struct humen sort = mass[i];
                mass[i] = mass[j];
                mass[j] = sort;
            }
        }
    }


    /*Выводим результат на экран*/
    printf("Результат: \n");
    for(int i = 0; i < n; i++)
    {
        printf("Имя = %s; Фамилия = %s; Год = %d\n", mass[i].Name, mass[i].Lastname, mass[i].Year);
    }
    
    
    return 0;
}
